<form method="post" action="{{ path('app_categories_delete', {'id': category.id}) }}" onsubmit="return confirm('êtes vous certain de vouloir supprimer cette Catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger mb-3">Supprimer</button>
</form>
