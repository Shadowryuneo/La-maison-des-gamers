<nav class="navbar navbar-expand-lg" style="background-color: #5C5D8D">
	<div class="container-fluid">
		<a class="navbar-brand text-white" href="{{ path('app_home') }}">La maison des gamers</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">

			<ul class="navbar-nav me-auto mb-2 mb-lg-0">

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Categories</a>
					{# value="{{'id': categories.id }}" #}
					{# {% for category in categories %} <a href="{{ path('app_categories_show', {'id': category.id}) }}">{{ category.name }}</a>
					{% endfor %} #}
					<ul class="dropdown-menu">
						{% for tabCategories in tabService.categories %}
							{# <li>
								<a class="dropdown-item" href="{{ path('app_Categories_category', {'id': tabCategories.id}) }}">{{tabCategories.nom}}</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_Categories_category', {'id': tabCategories.id}) }}">{{tabCategories.nom}}</a>
							</li> #}
							<li>
								<a class="dropdown-item" href="{{ path("app_categorie_id", {'id' : tabCategories.id}) }}">{{tabCategories.nom}}</a>
							</li>
						{% endfor %}
					</ul>
				</li>
			</ul>

			<ul class="navbar-nav ms-auto mb-2 mb-lg-0 me-3">
				<ul>
				{# {{ ux_icon('panier') }} #}
					<a class="nav-link text-white" href="{{ path('app_cart_index') }}">Panier</a>
				</ul>
				{% if app.user %}

					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle text-white me-5" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							{{ app.user.prenom }}
							{{ app.user.nom }}
						</a>
						<ul class="dropdown-menu">
							<li>
								<a class="dropdown-item" href="{{ path('app_profile_index') }}">Mon espace perso</a>
							</li>

							{# <li>
								<a class="dropdown-item" href="{{ path('app_mon_compte') }}">Mon espace perso</a>
							</li> #}

							{% if is_granted('ROLE_ADMIN') %}
								<li>
									<a class="dropdown-item" href="{{ path('app_admin') }}">Back office</a>
								</li>
							{% endif %}


							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
							</li>
						</ul>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link text-white" href="{{ path('app_register') }}">Inscription</a>
					</li>

					<li class="nav-item">
						<a class="nav-link text-white" href="{{ path('app_login') }}">Connexion</a>
					</li>
				{% endif %}

			</ul>

		</div>
	</div>
</nav>
