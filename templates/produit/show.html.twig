{% extends 'base.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="text-center">Produit</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ produit.id }}</td>
				</tr>
				<tr>
					<th>Prix</th>
					<td>{{ produit.prix }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ produit.description }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ produit.nom }}</td>
				</tr>
				{# <tr>
					<th>Image</th>
					<td>{{ produit.image }}</td>
				</tr> #}
				{# <tr>
					<th>Conssoles</th>
					<td>{{ produit.conssoles }}</td>
				</tr>
				<tr>
					<th>Jeuxvideo</th>
					<td>{{ produit.jeuxvideo }}</td>
				</tr> #}
			</tbody>
		</table>

		<a class="btn btn-primary mt-3 mb-3" href="{{ path('app_produit_edit', {'id': produit.id}) }}">Modifier</a>

		{{ include('produit/_delete_form.html.twig') }}

        <a class="btn btn-primary" href="{{ path('app_produit_index') }}">Retour a la gestion des produits</a>
	</div>
{% endblock %}
